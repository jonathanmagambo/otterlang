fn ackermann(m: float, n: float) -> float:
    if m == 0.0:
        return n + 1.0
    if n == 0.0:
        return ackermann(m - 1.0, 1.0)
    return ackermann(m - 1.0, ackermann(m, n - 1.0))

fn main():
    let result = ackermann(2.0, 1.0)

